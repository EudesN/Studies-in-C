# include <stdio.h>

int main(){
    int x;
    int len;
    
    printf("Informe o número de elementos no vetor: ");
    scanf("%d", &x);
    int vetor[x];

    for(i = 0; i < x; i++){

    }
    return 0;
}